import React, {FC} from 'react';
import {Text} from 'react-native';
import {ScreenWrapper} from '../../components';

const Profile: FC = () => {
  return (
    <ScreenWrapper>
      <Text>Profile</Text>
    </ScreenWrapper>
  );
};

export {Profile};
