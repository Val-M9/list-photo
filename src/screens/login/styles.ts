import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: 8,
    justifyContent: 'center',
  },
  heading: {
    paddingBottom: 50,
    textAlign: 'center',
    fontSize: 24,
    fontWeight: '700',
  },
  input: {
    marginBottom: 20,
  },
});

export {styles};
