import React, {FC} from 'react';

import {SafeAreaView, Text} from 'react-native';

const App: FC = () => {
  return (
    <SafeAreaView>
      <Text>Working</Text>
    </SafeAreaView>
  );
};

export default App;
