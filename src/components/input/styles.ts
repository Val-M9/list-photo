import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    paddingVertical: 6,
    paddingLeft: 15,
    paddingRight: 30,
    borderRadius: 5,
    lineHeight: 16,
    borderWidth: 1,
  },
});

export {styles};
