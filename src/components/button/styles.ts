import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  btn: {
    alignItems: 'center',
    paddingVertical: 10,
    paddingHorizontal: 30,
    borderRadius: 5,
  },
  btnText: {
    fontSize: 14,
    letterSpacing: 2,
    fontWeight: '700',
  },
});

export {styles};
